"use server"

import { User } from "./schema";
export const registerUser = async (user: User) => {
  // Fråga vad de här är nedan och vad  gör?
  // e.preventDefault();
    // const userInput = {};
    // const result = UserSchema.safeParse(userInput);
    console.log(user);
  }
